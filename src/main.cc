#include <iostream>
#include <fstream>
#include <queue> 

using namespace std;

int main(int argc, char** argv) {
  string fname = argv[0];
  read_file(fname);
	return 0;
}

string read_file(string fname) {
  
}

void fifo_scheduler() {
  
}
